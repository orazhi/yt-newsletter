# Tier 1 — RSS listing + transcript -> deep study notes -> HTML email.
# Channel listing uses the stdlib (urllib + xml.etree), so no yt-dlp needed here:
# RSS + the transcript API are unauthenticated and dodge the bot-wall.
youtube-transcript-api>=1.0.0
# Structured outputs (output_config.format) + adaptive thinking need a recent
# SDK — setup.sh installs the latest. Floor kept loose on purpose.
anthropic>=0.69.0

# Tier 2 (video download + keyframe vision) — uncomment when we build it.
# yt-dlp>=2026.6.9
# imageio-ffmpeg>=1.4.0
