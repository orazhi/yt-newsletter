# yt-newsletter — runtime dependencies
# Install with:  pip install -r requirements.txt   (see setup.sh)

# Video metadata, description, and subtitle/transcript extraction.
yt-dlp>=2026.6.9

# OPTIONAL — only for the PO-token transcript path (setup.sh BUILD_POT=1).
# This project uses cookies instead (config/cookies.txt), so this is not needed
# by default. Uncomment if you switch to the PO-token + www.google.com approach.
# bgutil-ytdlp-pot-provider>=1.3.0
